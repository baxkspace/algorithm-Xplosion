## BOJ 11651: KDH

## 문제 설명
- N개 (x, y) 좌표를 입력받는다.
- y 좌표 기준으로 오름차순 정렬을 하되, y 좌표가 같은 쌍이 있다면 x 좌표 기준으로 오름차순 정렬을 한다.

## 풀이 방법
- `pair<int, int>`를 이용해 좌표를 입력받는다.
- `sort()` 함수를 이용하되, `comparator()` 함수에서 x, y 좌표 정렬 기준을 정해 준다.